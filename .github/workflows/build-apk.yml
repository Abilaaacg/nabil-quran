name: Build Android APK

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Update version.json and changelog.json
        run: |
          echo "{\"version\": \"v${{ github.run_number }}\", \"url\": \"https://github.com/Abilaaacg/nabil-quran/releases/download/v${{ github.run_number }}/nabil-quran.apk\", \"size\": \"\"}" > public/version.json
          node -e "
          const fs = require('fs');
          const cl = JSON.parse(fs.readFileSync('public/changelog.json','utf8'));
          cl.version = 'v${{ github.run_number }}';
          fs.writeFileSync('public/changelog.json', JSON.stringify(cl));
          "

      - name: Build web app
        run: npm run build
        env:
          VITE_APP_VERSION: ${{ github.run_number }}

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup signing keystore
        run: |
          mkdir -p ~/.android
          if [ -n "$KEYSTORE_B64" ]; then
            echo "$KEYSTORE_B64" | base64 -d > ~/.android/release.keystore
            echo "✅ Keystore loaded from secret"
          else
            keytool -genkeypair -noprompt \
              -keystore ~/.android/release.keystore \
              -storepass android \
              -alias appkey \
              -keypass android \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Nabil Quran,O=NabilQuran,C=EG"
            echo ""
            echo "========================================"
            echo "⚠️  SAVE THIS AS GitHub Secret: KEYSTORE_BASE64"
            echo "========================================"
            base64 -w 0 ~/.android/release.keystore
            echo ""
            echo "========================================"
          fi
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_BASE64 }}

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build Release APK
        run: cd android && ./gradlew assembleRelease --no-daemon

      - name: Rename APK
        run: cp android/app/build/outputs/apk/release/app-release.apk nabil-quran.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: nabil-quran-apk
          path: nabil-quran.apk

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: "نور الإسلام v${{ github.run_number }}"
          body: |
            ## 📱 تطبيق نور الإسلام للأندرويد
            قم بتحميل ملف `nabil-quran.apk` وثبته على هاتفك
            > تحتاج لتفعيل "السماح بمصادر غير معروفة"
          files: nabil-quran.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy website to Netlify
        run: |
          cd dist
          zip -r ../dist.zip .
          cd ..
          curl -s -X POST \
            "https://api.netlify.com/api/v1/sites/bae8dbee-a4ef-428b-9f38-f806e20445ed/deploys" \
            -H "Authorization: Bearer $NETLIFY_TOKEN" \
            -H "Content-Type: application/zip" \
            --data-binary @dist.zip \
            | python3 -c "import sys,json; d=json.load(sys.stdin); print('Netlify deploy state:', d.get('state'))"
        env:
          NETLIFY_TOKEN: ${{ secrets.NETLIFY_TOKEN }}
